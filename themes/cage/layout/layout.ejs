<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <% var title=page.title; if (is_archive()){ title=__('archive'); if
    (is_month()){ title +='：' + page.year + '/' + page.month; } else if
    (is_year()){ title +='：' + page.year; } } else if (is_category()){
    title=__('count.categories') + '：' + page.category; } else if (is_tag()){
    title=__('count.tags') + '：' + page.tag; } %>
    <title><% if (title){ %> <%= title %> - <% } %> <%= config.title %></title>
    <% if (theme.favicon) { %>
    <link
      rel="shortcut icon"
      href="<%= theme.favicon.href %>"
      type="<%= theme.favicon.type %>"
    />
    <% } %>
    <link rel="stylesheet" href="https://unpkg.com/@highlightjs/cdn-assets@11.9.0/styles/github-dark.min.css">
    <link rel="stylesheet" href="https://unpkg.com/mdui@2/mdui.css" />
    <!-- Filled -->
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
    <%- css("css/layout") %>
  </head>
  <body>
    <div id="cage-top-header"></div>
    <%- include("components/loading") %>
    <div id="main-content" class="hidden-body ease-in">
      <%- include("components/header") %>
      <div class="cage-body">
        <%- include("components/sidebar") %>
        <div class="cage-content"><%- body %></div>
        <%- include("components/right-column") %>
      </div>
    </div>
    <script src="/lib/lazysizes.min.js"></script>
    <script async src="https://unpkg.com/@highlightjs/cdn-assets@11.9.0/highlight.min.js"></script>
    <script async src="https://unpkg.com/mdui@2/mdui.global.js"></script>
    <script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js">
    </script>
  </body>
</html>
